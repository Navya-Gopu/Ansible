---
- name: Check if host exists in tower inventory
  set_fact:
   fetch_get_info_post: "{{ name }}"
  # hosts: localhost
 
 - name: "Fail if Extra Variable does not exist in Ansible Tower's inventory"
        fail:
          msg: "{{ fetch_get_info_post }} does not exist in Ansible Tower's inventory."
